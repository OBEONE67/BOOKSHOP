<%- include('partials/header') %>

<!-- หนังสือแนะนำ (Recommended Books)
<section class="recommended-books">
    <h2>หนังสือแนะนำ</h2>
    <div class="book-list">
        <% if (typeof books !== 'undefined' && books.length > 0) { %>
            <% books.forEach(function(book) { %>
                <div class="book-item">
                    <img src="../uploads/<%= book.Photo %>" alt="<%= book.Title %>" width="100">
                    <h3><%= book.BookName %></h3>
                    <p><%= book.Title %></p>
                    <p><a href="">รายละเอียดเกี่ยวกับหนังสือ...</a></p>
                    <button>เพิ่มลงในตะกร้า</button>
                </div>
            <% }); %>
        <% } else { %>
            <p>No recommended books available at the moment.</p>
        <% } %>
    </div>
</section> -->

<%- include('partials/footer') %>

